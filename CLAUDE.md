# CLAUDE.md

이 파일은 새 세션마다 자동으로 읽힌다. 프로젝트의 목적, 작업 규칙, 현재 구조를 담는다.
**세션을 시작하면 이 파일을 읽은 뒤 `docs/progress.md`로 현재 위치를 확인하고 이어서 진행한다.**

---

## 프로젝트

가상의 K-뷰티 이커머스 고객사를 대상으로, 고객 문의를 자동으로 처리하는 **AI 에이전트**와 그 에이전트가 연동할 **고객사 시스템**을 직접 설계하고 구현한다.

제품을 만드는 것이 목적이 아니다. **B2B AI 고객 대응 솔루션을 고객사 환경에 붙이는 엔지니어(Forward Deployed Engineer)의 업무를 코드 수준에서 미리 경험하는 것**이 목적이다. 따라서 "동작하는 코드"보다 **"왜 이렇게 설계했고 무엇에 부딪혔는가"** 가 더 중요한 산출물이다.

## 목표

1. FDE가 정확히 어떤 일을 하는가
2. 구체적으로 어떤 업무를 수행하게 되는가
3. 그 과정에서 마주하게 되는 문제로는 어떤 것들이 있는가
4. AI 기반 고객 대응 시스템이 내부적으로 어떻게 동작하는가
5. 고객사 시스템이 일반적으로 어떻게 생겼고 어떻게 돌아가는가

## 작업자 배경

- 전통적인 백엔드 엔지니어링 경험이 있다. JVM 최적화까지 다뤄 봤으므로 **시스템 수준의 설명을 깊게 해도 된다.**
- LLM의 context rot 원리를 이미 이해하고 있다.
- LLM API를 호출해 응답을 서빙하는 웹 서비스를 만들어 본 경험이 있다.
- **LangGraph 등 에이전트 오케스트레이션 프레임워크는 처음이다.**
- Python은 쓴 지 오래됐다. JavaScript는 경험이 없다. **문법 학습은 이 프로젝트의 목적이 아니므로, 막히면 바로 알려 주고 넘어간다.**

---

## 설명 방식 — 반드시 지킬 것

### 1. 비유를 쓰지 않는다

다른 것에 빗대어 설명하지 않는다. 공식 용어를 그대로 쓰고, **정의 → 어떤 문제를 풀려고 나왔는지 → 실제로 어떻게 동작하는지** 순서로 설명한다. 단계를 건너뛰지 않는다.

구체적인 예시(실제 코드, 실제 입출력 값, 실제 데이터)는 비유가 아니므로 얼마든지 쓴다.

### 2. 구어 대신 공학 용어를 쓴다

| 쓰지 않는다 | 쓴다 |
|---|---|
| 락을 쥔다 | 락의 소유권을 획득한다 |
| 메모리를 잡는다 | 메모리를 할당한다 |
| 커넥션을 물고 있다 | 커넥션을 점유한다 |
| 요청이 밀린다 | 요청이 큐에 적체된다 |
| 프로세스가 죽는다 | 프로세스가 비정상 종료된다 |
| 고장 주입 | 장애 주입 |

표에 없어도 원리는 같다. 일상 대화의 동사로 기술 동작을 서술하지 않는다.

### 3. 모든 동작에 행위 주체를 명시한다

"락의 소유권을 획득해야 합니다"는 실패한 문장이다. "**그 스레드가** 락의 소유권을 획득해야 합니다"로 쓴다.
**문장 하나가 그 자체로 완결되어야 한다.** 읽는 사람이 "그래서 누가?"를 되묻게 만들면 실패다.

### 4. 한글 작문

- 실제 한국인의 발화·작문 형식을 따르고, 공식적인 상황의 어조(합니다체)를 쓴다.
- 지나친 한자어 축약("상기 건", "제반 사항")과 비어를 쓰지 않는다.
- 문장이 길어지면 끊는다. 명사를 이어 붙이는 대신 동사로 풀어 쓴다.

### 5. 저장소 문서에서는 특정 회사를 지칭하지 않는다

이 저장소는 공개되어 있다. 커밋되는 문서와 코드 주석에서 특정 회사의 이름이나 제품명을 쓰지 않는다.

---

## 진행 방식

아주 단순한 가정에서 출발해 점진적으로 구체화하는 **점진적 하향식 설계**를 뼈대로 한다.
각 층에서 다음을 반복한다.

```
현재 층의 그림을 그린다 (구성 요소는 상자로만, 내부는 비움)
    ↓
"지금 이 상태에서 무엇이 성립하지 않는가"를 찾는다
    ↓
최소 구현으로 실제로 한 번 돌려 본다
    ↓
설계상의 모순 + 구현에서 드러난 문제, 둘 다에서 다음 층을 도출한다
```

전체 경로와 세 번의 방식 전환은 `docs/00-approach.md`에 상세히 있다.

```
층 0  고객사와 코어 AI 엔진을 두고, CS 대응 자동화에 무엇이 필요한지 도출
층 1  두 시스템 간 연동 설계 및 구현 (가짜 인터페이스로 먼저 만들고 나중에 실제에 맞춰 고침)
층 2  코어 엔진 내부 분해 → 코어 루프 구현
      ↓ 전환 1: 고객사 시스템 파악 (주어진 것을 읽는 작업)
      ↓ 전환 2: 문의 300건 분류 → 고객사의 정책과 암묵지를 결정적 의사결정 체계로
층 3  연동부 분해 → 어댑터 구현
      ↓ 전환 3: 장애 주입
층 4  운영 층 (오퍼레이션 콘솔, 회귀 검증)
층 5  FDE 업무 온보딩 5단계 밟기
```

**각 층에는 시간 상한을 둔다.** 상한에 도달하면 미완성이어도 구현으로 넘어간다.

## 역할 분담

| 구성 요소 | 누가 작성하는가 | 근거 |
|---|---|---|
| 고객사 시스템 모의 구현 | Claude | FDE는 고객사 시스템을 만들지 않는다. 다만 **어디를 얼마나 지저분하게 만들지는 함께 정한다** |
| 코어 엔진 루프 | **작업자** | 에이전트 루프의 원리를 이해하는 것이 목적 |
| 고객사 어댑터 | **작업자** | FDE 업무 시간의 대부분이 여기 들어간다 |
| 규정 문서와 프롬프트 | **작업자** | 온보딩 2단계의 실체 |
| 평가와 회귀 검증 | 함께 | Claude가 뼈대, 작업자가 항목 |
| 오퍼레이션 콘솔 (JavaScript) | Claude | 경험 없는 언어이고 비중이 작다. 주석을 촘촘히 달아 읽을 수 있게 한다 |

**Claude가 코드를 쓸 때는 먼저 무엇을 왜 그렇게 만들지 설명하고 승인을 받은 뒤에 쓴다.** 다 만들어 놓고 결과만 보여 주면 이해가 남지 않는다.

## 그림 주고받기

설계 그림은 작업자가 그리고 Claude가 읽는다. **핵심 제약은 정확도가 아니라 오독의 조기 발견이다.**
Claude가 그림을 잘못 읽으면 조용히 틀린 이해로 진행하고, 작업자는 한참 뒤에야 이상함을 느끼게 된다.

### 도구

`excalidraw.com`에서 그리고 `docs/user-sketches/`에 두 형태로 저장한다.

- `layer-N.excalidraw` — 원본(JSON). **Claude는 이 파일을 직접 읽는다.** 이미지로 판독하지 않으므로 오독이 없고, 필요하면 Claude가 수정해서 의견을 같은 그림 위에 표시할 수 있다.
- `layer-N.png` — GitHub 웹에서 볼 이미지. 내보내기에서 "장면 포함" 옵션을 켠다.
- 텍스트는 소속되는 영역 이름표나 상자와 그룹으로 묶는다. 묶지 않으면 Claude가 소속 관계를 좌표로 추론해야 한다.

급할 때는 캡처만 붙여넣어도 된다. 손그림 사진보다 훨씬 정확하다.

### 왕복 절차

```
1. Claude가 질문을 던진다 ("이 상태에서 무엇이 성립하지 않습니까")
2. 작업자가 그리고 저장한 뒤 알린다
3. Claude가 읽은 것을 세 가지로 되짚는다
     (가) 읽은 것 — 상자 목록과 이름, 화살표 목록과 방향
     (나) 읽지 못한 것 — 반드시 명시한다. 생략하면 오독이 숨는다
     (다) 추론한 의도 — "이렇게 이해했는데 맞습니까"
4. 작업자가 정정한다
5. 확정되면 Claude가 Mermaid로 옮겨 층 문서에 남긴다
```

**3의 (나)를 생략하지 않는다.** 못 읽은 것을 못 읽었다고 말하는 것이 이 절차의 핵심이다.

**5에서 Mermaid는 두 역할을 겸한다.** Claude가 모든 상자와 화살표를 명시적으로 해석했다는 확인서이자, git이 층별 변화를 추적할 수 있는 텍스트 기록이다.

## 세션 운영

컨텍스트는 유한하다. 대화는 휘발성 저장소이고 파일은 영속 저장소다.

- **한 세션에 한 층.** 층이 끝나면 산출물을 `docs/`에 쓰고, 커밋하고, 새 세션을 연다.
- **긴 분석과 설계 결과는 대화에 남기지 않고 문서로 쓴다.**
- **참조 문서(스킬) 로드는 비싸다.** 정말 필요할 때만 부르고, 얻은 결론은 문서에 적어 다음 세션이 다시 부르지 않게 한다.
- 대화가 길어지면 Claude가 먼저 정리와 세션 분할을 제안한다.

## 디렉터리 구조

```
CLAUDE.md              이 파일 (세션 상시 컨텍스트)
README.md              프로젝트 소개
docs/
  00-approach.md       진행 방식 설계와 검증
  progress.md          현재 위치 — 세션 시작 시 반드시 읽는다
  problems.md          발견한 문제 목록 (목표 3의 최종 산출물)
  NN-layerN.md         각 층의 설계와 도출 과정
```

이후 코드 디렉터리는 층이 진행되면서 추가한다.

## 하지 말 것

- `.env`를 커밋하지 않는다. API 키가 공개 저장소에 올라가면 몇 분 안에 도용된다.
- 커밋 문서와 코드 주석에서 특정 회사를 지칭하지 않는다.
- 작업자가 직접 써야 할 코드를 Claude가 대신 쓰지 않는다. 막히면 설명하고 힌트를 준다.
- 설계 단계를 무한정 정교화하지 않는다. 상한에 닿으면 구현으로 넘어간다.

# 진행 상태

**세션을 시작하면 `CLAUDE.md`를 읽은 뒤 이 파일을 읽는다.**
층이나 전환이 끝날 때마다 이 파일을 갱신하고 커밋한다.

---

## 현재 위치

**층 0 완료. 층 1 시작 전.**

## 완료한 것

| 시점 | 내용 |
|---|---|
| 2026-08-20 | 진행 방식 설계 및 목표 달성 검증 (`docs/00-approach.md`) |
| 2026-08-20 | 저장소 생성, `README.md`, `.gitignore`, `.env.example` |
| 2026-08-20 | `CLAUDE.md` 작성 — 목적, 설명 규칙, 진행 방식, 역할 분담, 그림 주고받기 절차, 세션 운영 |
| 2026-08-20 | 스케치 보관 규칙 (`docs/user-sketches/`) |
| 2026-08-23 | **층 0** — 두 영역의 목적과 결핍 도출 (`docs/01-layer0.md`, `docs/user-sketches/layer-0.excalidraw`) |
| 2026-08-23 | `.excalidraw` JSON 판독 검증 통과 — 캡처 방식으로 되돌릴 필요 없음 |

## 다음에 할 일 — 층 1

연동 설계 및 구현. **설계 시간 상한: 40분.**

**질문**

> 문의가 엔진에 도달하고, 엔진이 알아낸 것을 고객사 시스템에서 실행하려면, 가운데 빈 영역과 그 양끝에 무엇이 있어야 하는가.

**층 0에서 넘어온 것** (`docs/01-layer0.md` 7절)

1. 가운데 영역을 채우는 연동 설계 — 가짜 인터페이스로 먼저 만든다 (00-approach.md 방법 B)
2. 소비자와 채널의 명시적 도출 — 층 0에서는 결핍 2 안에 암묵적으로만 존재한다
3. 가상 고객사 설정 확정 — 브랜드명, 상품군, 판매 채널 구성

**시뮬레이션이 예상한 도출** (실제와 비교할 것): 고객·주문 식별, 의도 판단, 규정 저장소, 답변 생성

**작도 시 유의**: 텍스트를 영역 이름표와 그룹으로 묶는다. 층 0에서 소속 관계를 좌표로 추론해야 했다.

## 가상 고객사 설정 (2026-08-23 확정)

| 항목 | 내용 |
|---|---|
| 브랜드 | 온글로우(OnGlow) — 가상 K-뷰티 브랜드 |
| 상품군 | 스킨케어 중심 (세럼, 크림, 선케어) |
| 판매 채널 | 자사몰 1곳 + 오픈마켓 1곳 |

상품군을 스킨케어로 정한 근거: 개봉 후 단순 변심 환불 제한, 피부 트러블 클레임, 유통기한 임박 재고처럼 CS 정책 쟁점이 풍부해서 전환 2(문의 300건 분류)의 재료가 자연스럽게 나온다.
채널을 둘로 정한 근거: 채널 간 정책·데이터 차이를 경험하면서도 층 3의 어댑터 작업량이 시간 상한 안에 들어온다.

## 미해결 사항

| 항목 | 내용 | 필요 시점 |
|---|---|---|
| Anthropic API 키 | 아직 발급받지 않았다. `console.anthropic.com`에서 발급 후 `.env`에 넣는다 | 층 2 코어 루프 구현 전 |
| `layer-0.png` | 캡처 원본이 임시 폴더에서 삭제되어 저장하지 못했다. excalidraw 내보내기("장면 포함")로 `docs/user-sketches/layer-0.png`를 저장한다 | 다음 excalidraw 접속 시 |

## 발견한 문제

목표 3의 산출물은 `docs/problems.md`에 누적한다. 아직 비어 있다.

## 환경 메모

- 저장소: https://github.com/leonroars/kbeauty-cs-agent (공개)
- git 이메일은 이 저장소에만 GitHub noreply 주소로 설정되어 있다. 전역 설정은 건드리지 않았다.
- `gh` CLI 설치 및 인증 완료 (`leonroars`)
- 작도 도구: excalidraw.com (무료, 로그인 불필요)
- 스케치 보관 위치: `docs/user-sketches/` (층 0에서 확정, 파일명은 `layer-N.*`)
